#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * _abs - computes the absolute value of n
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
